{
  "name": "cubaribot",
  "scripts": {
    "build": "tsc",
    "dev": "tsc-watch --onSuccess \"npm run start\"",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^3.0.0",
    "axios": "^1.5.0",
    "mongodb": "^5.7.0",
    "slash-create": "^5.13.0"
  },
  "devDependencies": {
    "@types/node": "^16.x",
    "rimraf": "^5.0.0",
    "tsc-watch": "^6.0.4",
    "typescript": "^4.0.0"
  }
}
